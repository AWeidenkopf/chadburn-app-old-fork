import { useState } from "react";

export const [leftRebuttalBtn, setLeftRebuttalBtn] = useState<boolean>(false);
export const [rightRebuttalBtn, setRightRebuttalBtn] = useState<boolean>(false);
export  const [playerBtn, setPlayerBtn] = useState<boolean>(false);
export const [psychicBtn, setPsychicBtn] = useState<boolean>(false);